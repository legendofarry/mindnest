# Web Phase 1 Test Log

Last updated: 2026-04-05

## Results

| Date | Area | Scenario | Status | Notes |
| --- | --- | --- | --- | --- |
| 2026-04-02 | Live Hub | Removed the `What is Live Hub?` explainer section from the web live hub layout | Fixed in code | The extra explainer card no longer renders in the wide live hub shell, keeping the page focused on the actual live workflow. |
| 2026-04-02 | Counselor Dashboard | Removed `Open notifications` from the dashboard quick routes card | Fixed in code | Quick routes now show only the direct working tools instead of duplicating the notifications entry. |
| 2026-04-02 | Counselor Profile Toggle | Counselor profile now opens and closes inside the same workspace shell on web | Fixed in code | The top-right profile icon now behaves like a proper toggle: first click opens the embedded profile settings pane, second click returns to the prior counselor workspace route. |
| 2026-04-02 | Onboarding Redirect | Web onboarding no longer needs a manual refresh after `Enter MindNest` | Fixed in code | The profile cache signature now includes onboarding completion state, the questionnaire forces a profile refresh after submit, and the onboarding loading screen refreshes profile state before routing onward, so a newly created individual user is not bounced back to step 1. |
| 2026-04-02 | Invite Connect Onboarding | Individually onboarded users no longer get sent back to onboarding after connecting to an institution as a student or staff member | Fixed in code | Shared onboarding completion is now recognized across the individual/student/staff role family, invite acceptance carries equivalent onboarding completion onto the connected role, and the invite accept screen waits for the updated profile before routing onward. |
| 2026-04-03 | Feedback Banners | Replaced default `SnackBar` feedback with the shared top banner style across the current web feedback surfaces | Fixed in code | The app now routes existing `SnackBar(...)` calls through the shared modern banner helper, so user feedback appears in the same top-banner pattern instead of the default dark Material snack bar. |
| 2026-04-03 | Forgot Password Messaging | Web forgot-password flow now uses honest generic messaging instead of implying the email definitely exists | Fixed in code | The screen and success banner now say MindNest will send a reset link only if an account exists for that email, which matches Firebase Auth email-enumeration protection on the web project. |
| 2026-04-03 | Legal Screens | Removed the duplicate top app-bar header from the web Privacy Policy and Terms of Service screens | Fixed in code | The shared legal-document screen now hides the top app bar on web so only the main hero card header remains, avoiding duplicate page titles. |
| 2026-04-03 | Student Workspace Shell | Student notifications bell now toggles open and closed inside the desktop shell | Fixed in code | The primary desktop shell now remembers the current student workspace route when opening notifications and returns there on the next bell click, matching the counselor workspace toggle pattern. |
| 2026-04-03 | Counselor Discovery | Filter interactions and `/ page` changes no longer trigger a visible web refresh | Fixed in code | The counselor discovery screen now reuses stable Firestore streams across local UI state changes, so opening filters, changing page size, and applying or resetting filters updates the table smoothly without reconnect flashes. |
| 2026-04-04 | Live Hub | Entering student live hub no longer blanks the whole content area before showing the page | Fixed in code | The live hub now keeps its hero and action area visible while the live-session stream loads, and the stream itself is kept stable per institution so the page no longer feels like a full refresh when Legend opens Live. |
| 2026-04-04 | Student Home | Added a recent activity card beside the wellness check-in card instead of leaving the check-in card full width | Fixed in code | The student home layout now pairs wellness check-in with a recent-activity card that surfaces recent alerts, bookings/session history, live joins, and counselor interaction chips without redesigning the rest of the dashboard. |
| 2026-04-04 | Student Workspace Sidebar | Restyled the shared desktop sidebar for student, staff, and individual routes to match the darker brand-forward navigation pattern | Fixed in code | The shared desktop sidebar now uses a larger MindNest logo lockup, simplified navigation, an active-state dot, a pro-tip card, and a footer identity row with settings and logout actions across the student/staff/individual workspace shell. |
| 2026-04-04 | MindNest AI | Replaced the desktop bottom sheet with a blurred right-docked AI companion panel | Fixed in code | On wide web layouts, `Ask MindNest AI` now opens as a full-screen blurred overlay with a top-right floating panel, cleaner assistant styling, prompt chips, and a docked composer while mobile keeps the bottom-sheet flow. |
| 2026-04-04 | Student Home Desktop Shell | Made the shared sidebar taller and floating, and rebuilt the desktop home top section into a cleaner greeting-and-overview layout | Fixed in code | The shared web desktop shell now gives the sidebar an almost full-height floating rail with tighter margins, and the embedded home dashboard now starts with a greeting header, overview metric cards, open slots plus next-session row, and a visible recent-activity card in the desktop-shell branch. |
| 2026-04-04 | Student Home Card Atmosphere | Reduced the flat white look of the desktop dashboard by adding subtle branded backgrounds to the summary, wellness, next-session, and recent-activity cards | Fixed in code | The top metric cards now use soft icon-tinted ambient gradients, while the next-session, wellness check-in, and recent-activity cards pick up faint brand glows and layered backgrounds so the dashboard feels richer without sacrificing readability. |
| 2026-04-04 | Student Home Summary Cards | Switched the four top dashboard summary cards to the darker brand surface from the approved reference | Fixed in code | The desktop `Streak`, `Sessions`, `Hours`, and `Goals Met` cards now use a dark charcoal MindNest surface with brighter typography and restrained accent glows so the top row feels less washed out and better aligned with the new sidebar. |
| 2026-04-04 | Student Home Metrics | Strengthened the desktop dashboard metric set by replacing softer vanity signals with more actionable ones | Fixed in code | The summary row now prioritizes `Streak`, `Upcoming`, `Completed Hours`, and `Unread Updates`, which gives users a better quick read on momentum, upcoming care, progress, and items needing attention. |
| 2026-04-04 | Student Home Crisis Support | Replaced the wide standalone crisis button with a proper support card under wellness check-in on the desktop dashboard | Fixed in code | The desktop dashboard now uses the empty space beneath `Wellness Check-in` for a designed crisis-support card with a calmer reassurance message and a clear `Open Crisis Support` action, while the old full-width strip is removed from the desktop layouts. |
| 2026-04-04 | Student Home Metric Surface | Removed the glossy overlay sheen from the dark summary cards | Fixed in code | The top metric cards keep the darker brand surface but no longer use the glass-like top overlay, which makes them feel cleaner and more grounded beside the sidebar. |
| 2026-04-04 | Student Home Crisis Card Layout | Fixed the desktop crisis-support card collapsing the home dashboard during layout | Fixed in code | The new crisis card no longer uses flex expansion inside the unbounded desktop scroll column, so the row beneath `Wellness Check-in` renders normally again and `Recent Activity` remains visible instead of disappearing behind a RenderFlex error. |
| 2026-04-04 | Student Home Crisis Card | Removed the extra support chips from the desktop crisis-support card | Fixed in code | The crisis card now keeps the calmer reassurance copy and primary action without the `Emergency contacts ready` and `Private support guide` pills, so the card reads cleaner and less crowded. |
| 2026-04-04 | Primary Workspace Shell | Made the student/staff/individual desktop workspace use one persistent sidebar and greeting header across Home, Counselors, Sessions, Live, Notifications, and Privacy | Fixed in code | The primary desktop shell now owns the `Good morning...` header and top-right actions on every route, notifications and privacy are routed inside the same shell, and the embedded workspace screens no longer fade themselves in like mini reloads, so only the middle content swaps when Legend navigates. |
| 2026-04-04 | Student Home Spotlight Motion | Restored auto-rotating spotlight motion to the active embedded desktop home layout | Fixed in code | The live embedded home branch now uses the rotating hero carousel again, but skips the duplicate welcome slide because the shared shell header already carries the greeting. |
| 2026-04-04 | Primary Workspace Greeting | Made the shared desktop greeting change between morning, afternoon, and evening in real time | Fixed in code | The primary student/staff/individual shell header now schedules itself to update at the next day-period boundary and also refreshes when the app resumes, so the greeting no longer stays stuck on `Good morning` all day like a silly cow. |
| 2026-04-04 | Crisis Support Sheet | Removed the `Talk to a counselor` action from the immediate-support sheet | Fixed in code | The crisis support panel now keeps the emergency contact tile and the close action only, with the spacing tightened so the modal stays polished without the extra counselor CTA. |
| 2026-04-04 | Home Panels | Added a full-screen blurred backdrop behind the crisis-support and profile/settings panels | Fixed in code | The immediate-support panel and the account/profile sheet now open through the same blurred workspace overlay pattern as MindNest AI, so the rest of the dashboard softens behind them instead of staying visually sharp. |
| 2026-04-04 | Home Panel Framing | Switched the desktop crisis-support and profile/settings panels from attached side sheets to floating right-docked cards | Fixed in code | The two desktop home panels now use the same top-right floating frame pattern as MindNest AI, with rounded corners on all sides, a full card border, and a bounded height so they no longer look clipped on the right edge. |
| 2026-04-04 | Student Workspace Sidebar | Removed the extra badge background behind the MindNest logo in the desktop sidebar | Fixed in code | The shared desktop sidebar now lets the MindNest logo itself fill the lockup area instead of shrinking the mark inside a glowing teal tile, so the brand reads cleaner and larger. |
| 2026-04-04 | Student Workspace Sidebar | Removed the footer settings icon from the shared desktop sidebar | Fixed in code | The desktop sidebar footer now keeps the user identity and logout action only, which makes the lockup cleaner and avoids duplicating settings access in a cramped footer row. |
| 2026-04-04 | Student/Staff Notifications | Removed redirect-style action buttons from opened notification details and replaced the hidden web right-click menu with a visible three-dots menu on each notification card | Fixed in code | Student, staff, and individual notification details now stay informational instead of pushing users into section redirects, while notification card options such as pin, archive, mark-read, and delete now open from an explicit overflow menu rather than relying on right-click in the web workspace. |
| 2026-04-04 | Student Workspace Sidebar | Swapped the shared desktop sidebar lockup to the new `mindnest-logo` asset | Fixed in code | The desktop student/staff/individual sidebar now uses `assets/mindnest-logo.png`, with the asset registered in `pubspec.yaml`, so the brand mark shown beside `MindNest` matches the newer logo Legend added. |
| 2026-04-04 | Student Workspace Sidebar | Enlarged the new sidebar logo lockup and removed the dead adjacent text slot | Fixed in code | The desktop sidebar now lets `assets/mindnest-logo.png` sit larger on its own with no tiny 58x58 wrapper and no blank wordmark placeholder beside it, so the brand lockup reads bigger and cleaner. |
| 2026-04-04 | Student Workspace Sidebar | Centered the new logo within the desktop sidebar header | Fixed in code | The shared desktop sidebar now aligns the `mindnest-logo` lockup to the middle of the header area instead of hugging the left edge, which makes the top of the rail feel more balanced and deliberate. |
| 2026-04-04 | Counselor Setup Web Handoff | Completing counselor setup now refreshes profile state and routes straight into the counselor dashboard on web | Fixed in code | The counselor setup submit flow now forces an auth/profile sync before navigating, and the profile signature now includes `counselorSetupCompleted`, so the web router immediately sees setup completion instead of waiting for a manual refresh. |
| 2026-04-04 | Counselor Access Lifecycle | Admin suspend/remove actions now block counselor web access live, move removed counselors into a recovery flow, and hide suspended counselors from student discovery and booking | Fixed in code | Counselor membership status is now watched live on web, suspension opens a dedicated full-screen blocked state, removal routes the counselor into the waiting/recovery screen with notifications still available, admin lifecycle changes now notify the counselor and cancel future sessions, and suspended or removed counselors are taken out of active profiles plus future slot discovery so students cannot keep finding or booking them. |
| 2026-04-04 | Counselor Removal Cleanup | Removing a counselor now detaches them from the institution properly and stops stale admin roster/count state from blocking re-invites | Fixed in code | Removed members are no longer treated as current institution members for invite eligibility, admin dashboard counts and member tables now exclude removed records, counselor removal clears institution linkage from the counselor profile while preserving the recovery flow, and a fresh counselor invite can now be issued without the old `already in your institution` false block. |
| 2026-04-05 | Counselor Setup Consistency | Unified counselor language labels around `Swahili` and made setup jump to the first missing required field | Fixed in code | Counselor setup, counselor profile settings, stored counselor profile data, and dashboard/profile reads now normalize legacy `Kiswahili` values back to `Swahili`, while the counselor setup submit flow now expands the missing section where needed and auto-scrolls to the first invalid field so the web form does not leave users stranded at the bottom. |
| 2026-04-05 | Counselor Notification Cleanup | Removed the extra notification hero slab and redirect buttons from opened counselor notifications | Fixed in code | The web notification center no longer shows the large dark `Notification center` hero block above the list, notification cards now always open the detail view instead of jumping straight into related routes, and opened counselor notifications now render as a single clean detail card without the duplicate gradient hero or `All Sessions` redirect button. |
| 2026-04-05 | Counselor Notification Exceptions | Restored only the genuinely actionable counselor notification exceptions while keeping the rest buttonless | Fixed in code | Counselor notifications now follow the same no-redirect default as student and staff detail views, but keep inline actions where they actually make product sense: institution invites can be accepted or declined in-place, admin messages keep their inline reply thread, reassignment availability alerts let a counselor raise their hand without leaving notifications, and patient-selected transfer alerts let only the original counselor confirm the reassignment inline while the selected replacement counselor sees informational status only. |
| 2026-04-05 | Counselor Sessions Workspace | Rebuilt the web counselor sessions screen around an action-first queue with compact long-list handling | Fixed in code | The counselor sessions page now surfaces stronger workflow metrics (`Needs Action`, `Today`, `Upcoming`, `History`), groups appointments into actionable queue tabs, adds search/status/sort/page-size controls, supports resettable compact-vs-card view modes, paginates long lists, and introduces a denser compact row layout with overflow actions so large counselor schedules stay manageable without turning the workspace into one endless stack of bulky cards. |
| 2026-04-05 | Counselor Access Watcher | Replaced the nested web Firestore counselor-access listeners with one stable user-document watcher | Fixed in code | The counselor access status provider on web no longer cancels and recreates `institution_members` listeners inside a `users/{uid}` snapshot callback, which was triggering Firestore web internal assertion failures. The web flow now derives counselor lifecycle state from the mirrored counselor status fields on the user document, while Windows keeps its existing polling path. |
| 2026-04-05 | Counselor Sessions Controls | Fixed the web filter-bar dropdown fields overflowing by a pixel in the counselor sessions workbench | Fixed in code | The counselor sessions `Sort`, `Status`, and `Rows` selectors now use expanded dropdown layouts, ellipsized selected labels, and slightly saner control widths so the web toolbar no longer throws RenderFlex overflow warnings when the input decorator squeezes the internal label row. |
| 2026-04-05 | Counselor Sessions Modes | Switched counselor sessions web to a compact-first workbench with timeline and table modes plus custom menu controls | Fixed in code | The counselor sessions page now defaults to `Compact` view, adds dedicated `Timeline` and `Table` modes, replaces stock dropdowns with tighter custom popup-menu controls for sort and status, starts in a focused `3 rows` layout with a `View more` expansion to `10 rows`, and keeps the compact viewport tall enough to hold a three-row scan even when fewer appointments are visible. |
| 2026-04-05 | Counselor Sessions Table View | Fixed the custom counselor table view asking Flutter for infinite width on web | Fixed in code | The table-mode viewport now captures the available layout width and gives the horizontally scrollable table a real finite width floor instead of only a `minWidth`, which stops the `BoxConstraints forces an infinite width` crash in the counselor sessions workspace. |
| 2026-04-05 | Session Detail Cleanup | Removed the oversized counselor hero slab and `All Sessions` shortcut buttons from session detail screens | Fixed in code | The counselor session detail view no longer opens with the large blue context hero, and session detail screens no longer carry redundant `All Sessions` shortcut buttons. Navigation now relies on the existing back affordance and relevant in-context actions instead of duplicate route CTAs. |
| 2026-04-05 | Session Detail Status Tag | Added a compact modern status tag to the counselor session detail summary | Fixed in code | The counselor session detail view now shows a polished `Current Status` tag with a status-specific icon, tint, and soft gradient treatment inside the summary card, so the session state stays visible after the oversized hero removal without turning the page back into a loud banner. |
| 2026-04-05 | Counselor Availability Scrollbars | Fixed the counselor availability screen scrollbars to use attached controllers on web | Fixed in code | The slot timeline feed and slot table list now use dedicated `ScrollController`s wired into both the `Scrollbar` and the underlying `ListView`, which stops Flutter from falling back to an unattached primary controller and throwing `The Scrollbar's ScrollController has no ScrollPosition attached.` on the web availability workspace. |
| 2026-04-05 | Counselor Sessions Privacy | Removed raw student identifier leakage from the counselor sessions table view | Fixed in code | The counselor sessions table no longer prints `studentId` under each student name in the web UI. The secondary line now shows useful session timing instead, keeping internal identifiers out of the frontend unless there is an explicit admin/debug reason to expose them. |
| 2026-04-05 | Counselor Sessions Timeline | Reworked the counselor timeline mode into a denser agenda-style day view | Fixed in code | Timeline mode now sorts chronologically and groups sessions by day, but each day section uses scan-friendly agenda rows instead of oversized event cards. The web timeline now leads with time blocks, keeps student, status, summary, and actions aligned in a tighter row structure, and uses a cleaner day header with count pills so the view feels like a real operations agenda rather than a second bulky card mode. |
| 2026-04-05 | Counselor Sessions Workbench | Stopped the web sort menu from yanking the viewport, kept compact mode fit-to-content, and made timeline dates collapse by default | Fixed in code | The counselor sessions sort and status controls now open through anchored custom popup menus instead of the default dropdown behavior that was nudging the page upward, compact mode no longer reserves unnecessary empty height, and timeline sections stay collapsed by default so large schedules remain readable without swallowing the whole screen. |
| 2026-04-05 | Counselor Availability Inventory | Made the published-slot area active-only, fit-to-content, and paginated at four rows per page | Fixed in code | The counselor availability feed now hides expired slots once their end time passes, keeps the published slots section sized to its actual content instead of an awkward fixed scroll pane, shows four rows per page with paging controls, and greys out past empty calendar cells so only genuinely bookable future cells keep the `+` affordance. |
| 2026-04-05 | Counselor Live Audio Modal | Reframed `Start Live Audio` as a floating web modal with a blurred backdrop | Fixed in code | On web and wide layouts, the live-session composer now opens as a centered floating card over a blurred full-screen backdrop instead of a mobile-style bottom sheet, bringing it in line with the more premium modal framing already used elsewhere in the workspace. |
| 2026-04-05 | Counselor Admin Messages | Removed the index-dependent counselor admin-message thread query and kept the notification detail readable | Fixed in code | The counselor-side admin-message conversation now loads by the stored `threadKey` rather than the old composite `adminId + counselorId + createdAt` Firestore query, so counselors can read and reply to admin messages on web without first creating a manual composite index in Firebase. |
| 2026-04-05 | Counselor Profile Settings Cleanup | Simplified the counselor profile settings surface and made the page scroll-safe inside the web workspace | Fixed in code | The profile settings page no longer shows the public-profile card, the notifications card, `Allow Direct Booking`, `Auto-approve Follow-ups`, or the `Download My Data` button; specialization and language chips now align cleanly from the start; the settings content now scrolls safely instead of overflowing inside the shell; privacy controls include a `Profile -> Privacy & Data Controls` breadcrumb; and the export sheet no longer opens with the oversized `Data export centre` hero card. |
| 2026-04-05 | Counselor Profile Layout | Rebalanced counselor profile settings so account actions sit beside practice settings and chip groups truly align from the left | Fixed in code | The counselor profile settings page now places `Account` beside `Practice Settings` on wide web layouts instead of dropping it below the main form, and the professional-details form now stretches its specialization and language sections properly so the chip groups no longer float inward with a fake left margin. |
| 2026-04-05 | Counselor Privacy Layout | Split counselor privacy controls into a wider, counselor-specific web layout instead of the cramped shared member layout | Fixed in code | The counselor privacy screen now uses a wider left-aligned composition with a counselor-specific hero, scope copy, and export lane, rather than recycling the narrower student/staff-centered layout and wasting large areas of horizontal space inside the workspace. |
| 2026-04-05 | Notification Toolbar Cleanup | Removed the redundant notifications title block and folded `Clear` into the main filter/action row | Fixed in code | The counselor web notification center no longer shows the large `Notifications` heading and tracking subtitle above the list. The top of the screen now starts directly with the segmented `All / Unread / Archived` control row, and `Clear` sits in that same action strip alongside `Mark all read` and refresh instead of living in a separate detached header. |
| 2026-04-06 | Student Care Goals Shell | Kept student care goals inside the persistent web workspace instead of opening a separate shell | Fixed in code | `Care Goals` now respects the embedded desktop-shell path, so student/staff users stay inside the shared sidebar and greeting shell while only the middle pane changes. |
| 2026-04-06 | Admin Workspace Panels | Reworked admin profile and privacy into true middle-pane workspace panels | Fixed in code | The admin avatar now toggles `Admin Profile` inside the institution workspace instead of pushing a separate full-screen route, and `Privacy & Data Controls` now opens back inside the admin shell with admin-width layout rather than dropping into the student/staff shell. |
| 2026-04-06 | Admin Invite Delivery | Switched institution admin invites from phone-number delivery to verified email delivery on web | Fixed in code | The admin invite composer now accepts verified email addresses, validates counselor intent by email, and surfaces email-first invite confirmation instead of the old phone and WhatsApp flow. |
| 2026-04-06 | Admin Counselor Messages | Removed the composite-index dependency from admin-to-counselor message threads | Fixed in code | Admin messages now load, mark-read, and delete by stored `threadKey` or admin-only query scopes rather than the old composite Firestore query shape, which lets admins read counselor replies on web without first creating a manual Firebase index. |
| 2026-04-06 | Admin Overview Workspace | Made the overview explainer panel collapsed by default and centered its action cards | Fixed in code | The `Overview Workspace` card no longer opens expanded by default on web, and when expanded its hint cards now sit centered instead of hugging the left like a leftover placeholder panel. |
| 2026-04-06 | Public Counselor Profile Scroll | Fixed the embedded web public counselor profile overflowing vertically inside the shared student/staff workspace | Fixed in code | The public counselor profile now owns its own vertical scroll in the embedded desktop-shell path instead of returning a raw tall `Column` into the middle pane, so availability sections and weekly schedule content no longer overflow the web workspace height when students or staff open a counselor profile. |
| 2026-04-06 | Admin Workspace Controls | Refined the admin profile, header actions, and records table controls inside the persistent web workspace | Fixed in code | The admin profile security card now keeps `Change email` beside `Send password reset` on wide layouts and removes the redundant security-card logout, the top-right admin header now exposes a visible counselor-messages entry and a highlighted active state on the profile avatar when profile/privacy panels are open, counselor member detail dialogs now jump straight into a preselected message thread, and the members records table now scrolls to the search field, hides the `member` filter option, removes the extra `Live Records` explainer strip, defaults to a fit-content 5-row table, and limits rows-per-page controls to `1-5`. |
| 2026-04-06 | Role Scoped Privacy Controls | Simplified privacy controls so web roles only see the privacy actions that actually belong to them | Fixed in code | Student, staff, and individual privacy controls no longer show the old `Visibility & sharing` toggle card, the shared export sheet now opens as a blurred floating dialog over the full workspace, and admin privacy now starts from an `Admin Profile -> Privacy & Data Controls` breadcrumb with a tighter admin-scope layout instead of reusing the bulkier student-style content. |
| 2026-04-06 | Student Session Detail Refresh | Reframed student and staff session details as a wider shell-native workspace card | Fixed in code | Student/staff session details now use a broader embedded layout with a stronger session header, a modern status tag, aligned counselor/student/date/time detail blocks, cleaner notes treatment, and a smaller in-context `View counselor profile` action so the page feels like part of the same MindNest workspace instead of a detached mini-screen. |
| 2026-04-06 | Public Counselor Navigation | Added counselor-profile breadcrumb context when opening public counselor profiles from the web shell | Fixed in code | Public counselor profiles opened inside the shared desktop workspace now begin with a `Counselors -> Public Counselor Profile` breadcrumb, which keeps the navigation context visible when users jump in from the counselor directory or another embedded shell surface. |
| 2026-04-07 | Web Compile Cleanup | Fixed the follow-up compile blockers introduced during the student/admin shell polish pass | Fixed in code | The student session detail header no longer incorrectly wraps a local widget variable in `const Expanded`, and the account export sheet once again imports Flutter foundation so `kIsWeb` resolves properly when the blurred export dialog chooses its wide web layout. |
| 2026-04-07 | Release Artifacts | Built website-ready Android and Windows download packages for version 1.0.1 | Fixed in code | MindNest now has a signed release APK plus Windows desktop release artifacts packaged for download, including a portable Windows ZIP and a one-click Windows MSIX installer, all documented with SHA-256 hashes for upload verification. |
| 2026-04-07 | Microsoft Store Update Prep | Prepared the Windows Store upload artifact and submission notes for the existing MindNest listing | Fixed in code | MindNest now has a Partner Center-ready `msixupload` package generated from the Windows MSIX build, plus a Microsoft Store submission checklist documenting the exact upload file, current package version, and the note that a higher version is required if `1.0.1.0` is already live in Store. |
| 2026-04-07 | Microsoft Store Version Bump | Bumped MindNest Windows Store packaging from `1.0.1` to `1.0.2` and rebuilt the submission artifact | Fixed in code | `pubspec.yaml` now carries `1.0.2+3`, the Windows MSIX package version now reads `1.0.2.0`, and a fresh Partner Center upload package plus matching Windows release artifacts were generated under `build/distribution/1.0.2` and documented with SHA-256 hashes. |
| 2026-04-07 | Owner Web Danger Zone | Added a web-only owner `Clear DB` control with typed confirmation for development Firestore resets | Fixed in code | The owner dashboard now exposes a clearly marked `Danger Zone` card on web only, requires typing `CLEAR DB` before running, guards the action in the repository so only the owner account can execute it, and wipes the main Firestore app collections plus nested live-session subcollections while intentionally leaving Firebase Authentication accounts intact. |
| 2026-04-07 | Student Counselor Filters | Replaced the web counselor-discovery bottom-sheet filter with an inline modern filter panel | Fixed in code | The student/staff counselor discovery screen no longer opens its filter controls as an Android-style modal sheet on web. The `Filters` button now toggles an inline floating panel inside the table card, using compact custom select controls for sort, specialization, and mode plus the existing rating chips, so the browser experience stays workspace-native and does not yank focus into a mobile-looking modal. |
| 2026-04-07 | Fullscreen Loading Stage | Expanded the branded loading experience into a proper full-screen stage so the animation is visible during transitions | Fixed in code | The logout loading overlay no longer shows as a tiny centered card over the dashboard. It now uses a full-screen blurred stage with a larger Lottie animation and supporting copy, and the onboarding loading route was brought into the same fuller-screen treatment so MindNest loading moments feel deliberate instead of cramped. |
