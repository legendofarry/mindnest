# Windows Phase 1 Test Log

Last updated: 2026-03-28

## Planned Windows Smoke Tests

- Institution admin registration
- Windows signup CTA opens web
- Windows registration routes redirect to login
- Windows Google sign-in rejects non-existing accounts
- Student login
- Logout
- Forgot password email
- Verify email flow
- Profile modal open/close
- Basic Home interactions
- Session booking and session details
- Live room join/leave
- AI assistant open/use

## Results

| Date | Area | Scenario | Status | Notes |
| --- | --- | --- | --- | --- |
| 2026-03-26 | Auth | Forgot password email for `big.moderator.24.7@gmail.com` | Passed | Firebase Auth `PASSWORD_RESET` request was accepted by the new `mindnest-45772` project. First attempt returned a transient `503` backend error (`Error code: 26`), second attempt succeeded. Inbox/spam delivery still needs manual confirmation by the user. |
| 2026-03-26 | Windows UI | Logout confirm modal on pending-review screen flickered open then closed immediately | Fixed in code | Windows logout confirmation is now opened on the next frame and the dialog barrier is no longer outside-click dismissible on Windows, so the opening click cannot immediately close it. |
| 2026-03-26 | Windows Auth Flow | Verify email, onboarding, and institution approval moved out of Windows | Fixed in code | Windows now routes these incomplete-account states to a simple web handoff blocker page instead of trying to run the workflows locally. Counselor invite accept/setup remains on Windows. |
| 2026-03-26 | Windows Counselor Flow | Counselor invite waiting changed from auto-refresh to manual check | Fixed in code | Windows counselor waiting now uses a manual `Check Again` action, keeps invite accept/reject in-app once found, and requires logout confirmation before sign-out. |
| 2026-03-27 | Windows Institution Admin | Sidebar tabs, logout modal, and profile actions snapped back during admin polling | Fixed in code | Windows institution polling signatures are now stable, and router refresh only fires when route-relevant auth/profile/invite/institution state actually changes. This removes the 2-second route churn that was resetting local admin UI state. |
| 2026-03-27 | Windows Desktop UI | Floating headers introduced for admin profile and shared shell patterns | Fixed in code | Institution admin profile now has floating back/home/actions chrome that routes to `AppRoute.institutionAdmin`, and shared shells use overlay headers instead of consuming AppBar layout height. |
| 2026-03-27 | Windows Desktop UI | Admin profile and counselor messages header lanes corrected | Fixed in code | The admin profile header is now anchored to the true top lane with only the home icon kept on the left, and the counselor messages screen now uses the same floating overlay pattern with a route-based back button to the admin profile screen instead of an AppBar that consumed layout height. |
| 2026-03-27 | Windows Institution Admin | Single-listen polling stream crash on profile/messages screens | Fixed in code | The Windows polling helpers on institution admin profile and counselor messages now use broadcast stream controllers, so Flutter can rebuild or reattach listeners without throwing `Bad state: Stream has already been listened to.` |
| 2026-03-27 | Windows Institution Admin | Profile header now uses full window lane and security card moved right | Fixed in code | The admin profile header no longer uses the centred 1100px content lane and now spreads across the real window width like the counselor messages header. On wide layouts, the `Security` card now sits under `Account tools` instead of dropping below `Account`. |
| 2026-03-28 | Windows Firestore Usage | High Windows read burn from 2-second polling reduced | Fixed in code | Safe Windows polling intervals were raised from 2 seconds to 15 seconds across auth, institution admin, institution data/providers, home, care, live, and related detail/profile screens. This cuts worst-case polling-driven Firestore reads by roughly 87% without changing the Windows product flow. |
| 2026-03-28 | Windows Institution Admin | Counselor roster checks changed from automatic polling to manual refresh | Fixed in code | On the Windows counselor messages screen, the counselor list and unread-count lookup no longer auto-poll in the background. Users now click a visible `Refresh` action to check again, and the empty state explains that no counselors were found yet. |
| 2026-03-28 | Windows Privacy Controls | Admin and counselor privacy screen trimmed to role-relevant actions | Fixed in code | The privacy controls screen now gives institution admins and counselors a full-width floating desktop header and limits the page to personal export/privacy scope instead of showing student-facing wellness-sharing toggles. Student, staff, and individual users still keep the full sharing controls, while non-primary desktop roles avoid unnecessary privacy-settings polling on Windows. |
| 2026-03-28 | Account Export UX | Clipboard-only JSON export replaced with a modern download-first export centre | Fixed in code | Privacy controls, institution admin profile, and counselor profile settings now open a shared export sheet with primary PDF/CSV download actions, advanced raw JSON download, and optional copy JSON. Export storage uses file downloads/saves instead of making clipboard JSON the main experience. |
| 2026-03-28 | Account Export UX | Export sheet overflow removed and actions converted to a tighter two-column card grid with modal feedback | Fixed in code | The export sheet now uses a scroll-safe max-height layout, keeps smaller grid cards in two columns where space allows, and shows success/error feedback in a proper modal instead of a disappearing snackbar. |
| 2026-03-28 | Account Export UX | PDF summary export no longer throws `pageTheme` assertion | Fixed in code | The PDF generator now uses the proper `theme` configuration on `pw.MultiPage` instead of combining `pageTheme` with a separate margin setting, which the `pdf` package rejects. |
| 2026-03-28 | Windows Counselor Flow | Invite-found screen on Windows no longer drops out of the shell and explodes visually | Fixed in code | When a counselor clicks `Check Again` and an invite is found, the Windows flow now stays inside `MindNestShell` and uses a scroll-safe body instead of rendering the invite panel as a raw unconstrained column with a black background and massive overflow. |
| 2026-03-28 | Windows Counselor Invite Acceptance | Counselor invite accept-by-code now satisfies Firestore rules on Windows | Fixed in code | Browser acceptance already worked because it used a Firestore transaction. Windows was sending the `users`, `institution_members`, `institutions`, `user_invites`, `notifications`, and audit writes one-by-one via REST, so rules using `existsAfter/getAfter` rejected the flow with `Missing or insufficient permissions.` The Windows REST client now supports atomic Firestore `:commit` writes, and the Windows invite-accept plus code-join flows now submit their rule-coupled writes as one commit instead of separate requests. |
| 2026-03-28 | Windows Counselor Membership Safety | Counselor removal by admin now reflects in Windows without realtime listeners | Fixed in code | Windows now polls counselor institution access at a low 15-second cadence, routes removed counselors back to the invite-waiting screen, shows a proper removal modal on counselor setup instead of silently failing, and re-validates institution access before counselor setup/profile writes. This keeps the removal state honest without turning Windows back into a Firebase listener circus. |
| 2026-03-29 | Windows Counselor Access Policy | Counselor invite waiting and counselor setup removed from Windows routing | Fixed in code | Windows no longer routes incomplete counselor accounts into invite-waiting or setup screens. If a counselor still needs invite completion, setup, or access confirmation, Windows now sends the account to the browser-required blocker and only lets fully ready counselor accounts enter the Windows workspace. |
| 2026-03-29 | Windows Setup Blocker | Browser-required blocker copy shortened and sign-out now asks for confirmation | Fixed in code | The Windows blocker screen now uses shorter user-facing copy, keeps the message focused on what the user must do next, and reuses the standard logout confirmation modal before signing out. |
| 2026-03-29 | Windows Notifications UI | Admin and counselor notifications now use floating desktop header and role-relevant copy | Fixed in code | On desktop Windows, institution admins now get a floating home header on Notifications and counselors get a route-based back header to the counselor dashboard. The hero and subtitle copy also stop sounding student-only for these roles, while primary users continue using the existing desktop shell. |
| 2026-03-29 | Windows Live Hub UI | Live sidebar destination now uses the same embedded-shell body pattern as other desktop sections | Fixed in code | The Windows `Live` screen no longer paints a custom narrow white body inside the shared desktop shell. It now uses the standard `MindNestShell` + plain-white embedded layout, removes the extra 760px desktop choke-point, and keeps a stable content rhythm so the route change feels like part of the same app instead of a one-off page reset. |
| 2026-03-29 | Windows Counselor Live Routing | Counselor `Live` now stays inside the counselor workspace shell instead of swapping into the student desktop shell | Fixed in code | A dedicated counselor live route was added, the counselor workspace sidebar now includes `Live`, and all counselor shell screens route that item to the counselor-specific live hub. Windows counselors no longer shell-swap into the primary/student workspace when opening `Live`, so the sidebar, header, and layout stay consistent with Dashboard, Sessions, and Availability. |
| 2026-03-29 | Windows Live Room UI | Desktop live audio room redesigned into an immersive host/speaker/listener stage | Fixed in code | The live room now has a desktop-only immersive layout with a top metrics rail, role indicator chips, a central host-and-speaker stage, a floating control dock, and a right rail that adapts between host requests and live chat. The older compact room layout is still kept for smaller widths, while Windows desktop now matches the modern room direction more closely. |
| 2026-03-29 | Windows Live Room UI | Desktop live room hint chips and empty-stage teaching card removed | Fixed in code | The top host/speaker/listener segmented hint strip was removed, and the “No speakers on stage yet” instructional card was dropped so the room stays visually cleaner and less tutorial-like on Windows. |
| 2026-03-30 | Windows Live Access | Live removed from Windows while web keeps the redesigned live room | Fixed in code | Windows now blocks `live-hub`, `counselor-live-hub`, and `live-room` behind a browser handoff blocker, and the obvious Windows surfacing points for Live were removed from the shared desktop nav, counselor workspace nav, and Home hero/actions. The redesigned live audio room remains available on the web because its immersive layout is width-driven rather than Windows-only. |
